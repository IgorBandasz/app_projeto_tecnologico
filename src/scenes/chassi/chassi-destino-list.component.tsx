import React from 'react';
import ContentView from '../../layouts/chassi/chassi-destino-list.component';

export const ChassiDestinoScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);