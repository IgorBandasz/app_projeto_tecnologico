import React from 'react';
import ContentView from '../../layouts/historico-pneu/historico.component';

export const HistoricoPneuScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
