import React from 'react';
import ContentView from '../../layouts/movimentacao-pneu/option.component';

export const MovimentacaoPneuScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);