import React from 'react';
import ContentView from '../../layouts/movimentacao-pneu/acao.component';

export const MovimentacaoPneuAcaoScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);