import React from 'react';
import ContentView from '../../layouts/filial/list.component';

export const FilialScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);