import React from 'react';
import ContentView from '../../layouts/centro-custo/list.component';

export const CentroCustoScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);