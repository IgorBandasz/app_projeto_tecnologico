import React from 'react';
import ContentView from '../../layouts/conserto-pneu/add.component';

export const ConsertoPneuAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
