import React from 'react';
import ContentView from '../../../layouts/conserto-pneu/servico/add.component';

export const ConsertoPneuServicoAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
