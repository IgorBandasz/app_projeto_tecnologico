import React from 'react';
import ContentView from '../../../layouts/conserto-pneu/servico/view.component';

export const ConsertoPneuServicoViewScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
