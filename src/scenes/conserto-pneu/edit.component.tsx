import React from 'react';
import ContentView from '../../layouts/conserto-pneu/edit.component';

export const ConsertoPneuEditScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
