import React from 'react';
import ContentView from '../../layouts/lancamento-contador/add.component';

export const LancamentoContadorAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
