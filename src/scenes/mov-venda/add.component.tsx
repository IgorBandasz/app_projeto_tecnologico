import React from 'react';
import ContentView from '../../layouts/mov-venda/add.component';

export const MovVendaAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
