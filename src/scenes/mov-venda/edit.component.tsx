import React from 'react';
import ContentView from '../../layouts/mov-venda/edit.component';

export const MovVendaEditScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
