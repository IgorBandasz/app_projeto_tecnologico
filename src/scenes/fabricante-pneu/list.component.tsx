import React from 'react';
import ContentView from '../../layouts/fabricante-pneu/list.component';

export const FabricantePneuScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);