import React from 'react';
import ContentView from '../../../layouts/recapagem-pneu/servico/add.component';

export const RecapagemPneuServicoAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
