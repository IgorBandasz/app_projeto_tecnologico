import React from 'react';
import ContentView from '../../../layouts/recapagem-pneu/servico/view.component';

export const RecapagemPneuServicoViewScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
