import React from 'react';
import ContentView from '../../layouts/recapagem-pneu/edit.component';

export const RecapagemPneuEditScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
