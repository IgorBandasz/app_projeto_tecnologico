import React from 'react';
import ContentView from '../../layouts/recapagem-pneu/add.component';

export const RecapagemPneuAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
