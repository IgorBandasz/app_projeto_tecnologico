import React from 'react';
import ContentView from '../../layouts/fornecedor/list.component';

export const FornecedorScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);