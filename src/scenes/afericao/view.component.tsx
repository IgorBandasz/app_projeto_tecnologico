import React from 'react';
import ContentView from '../../layouts/afericao/view.component';

export const AfericaoViewScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
