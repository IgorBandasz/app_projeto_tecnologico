import React from 'react';
import ContentView from '../../layouts/afericao/option.component';

export const AfericaoScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);