import React from 'react';
import ContentView from '../../layouts/afericao/edit.component';

export const AfericaoEditScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
