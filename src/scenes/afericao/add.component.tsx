import React from 'react';
import ContentView from '../../layouts/afericao/add.component';

export const AfericaoAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
