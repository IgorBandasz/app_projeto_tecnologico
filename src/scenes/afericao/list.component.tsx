import React from 'react';
import ContentView from '../../layouts/afericao/list.component';

export const AfericaoListScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);