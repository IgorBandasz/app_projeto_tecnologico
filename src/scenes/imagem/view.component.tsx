import React from 'react';
import ContentView from '../../layouts/imagem/imagem.view';

export const ImagemViewScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
