import React from 'react';
import ContentView from '../../layouts/medida/list.component';

export const MedidaScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);