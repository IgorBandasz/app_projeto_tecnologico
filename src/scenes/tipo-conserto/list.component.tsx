import React from 'react';
import ContentView from '../../layouts/tipo-conserto/list.component';

export const TipoConsertoScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);