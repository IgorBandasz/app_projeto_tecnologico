import React from 'react';
import ContentView from '../../layouts/pneu/add.component';

export const PneuAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
