import React from 'react';
import ContentView from '../../layouts/pneu/view.component';

export const PneuViewScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
