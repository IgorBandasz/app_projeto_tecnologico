import React from 'react';
import ContentView from '../../layouts/pneu/list.component';

export const PneuScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);