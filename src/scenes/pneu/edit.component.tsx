import React from 'react';
import ContentView from '../../layouts/pneu/edit.component';

export const PneuEditScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
