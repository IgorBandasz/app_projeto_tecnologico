import React from 'react';
import ContentView from '../../layouts/mov-estoque/edit.component';

export const MovEstoqueEditScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
