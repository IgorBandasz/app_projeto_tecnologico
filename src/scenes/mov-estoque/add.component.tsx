import React from 'react';
import ContentView from '../../layouts/mov-estoque/add.component';

export const MovEstoqueAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
