import React from 'react';
import ContentView from '../../layouts/motivo/list.component';

export const MotivoScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);