import React from 'react';
import ContentView from '../../layouts/modelo-pneu/list.component';

export const ModeloPneuScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);