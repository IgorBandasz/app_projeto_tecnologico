import React from 'react';
import ContentView from '../../layouts/mov-sucata/add.component';

export const MovSucataAddScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
