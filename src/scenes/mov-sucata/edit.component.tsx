import React from 'react';
import ContentView from '../../layouts/mov-sucata/edit.component';

export const MovSucataEditScreen = ({ navigation, route }): React.ReactElement => (
  <ContentView navigation={navigation} route={route}/>
);
